# Used by GoReleaser dockers_v2: copies the prebuilt per-platform binary.
FROM gcr.io/distroless/static-debian12:nonroot
ARG TARGETPLATFORM
COPY $TARGETPLATFORM/discloud /discloud
USER nonroot
EXPOSE 8080
ENTRYPOINT ["/discloud"]
